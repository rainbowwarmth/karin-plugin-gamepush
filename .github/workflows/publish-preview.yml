name: Publish Preview

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: write

jobs:
  preview:
    runs-on: ubuntu-latest
    # if 条件直接写表达式，无需 ${{ }}（已保留你的修正）
    if: startsWith(github.event.pull_request.title, 'chore(main): release')

    steps:
      # 1️⃣ 检出代码
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ 设置 Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ 安装依赖
      - name: Install dependencies
        run: npm install

      # 4️⃣ 生成随机预览版本号
      - name: Generate preview version
        run: |
          VERSION=$(jq -r .version package.json)
          RANDOM_STR=$(node -e "const c='0123456789abcdefghijklmnopqrstuvwXYZ';let s='';for(let i=0;i<8;i++)s+=c[Math.floor(Math.random()*c.length)];console.log(s)")
          PREVIEW_VERSION="${VERSION}-${RANDOM_STR}"
          echo "PREVIEW_VERSION=$PREVIEW_VERSION" >> $GITHUB_ENV
          npm version "$PREVIEW_VERSION" --no-git-tag-version
        # 避免 RANDOM 与系统环境变量冲突，重命名为 RANDOM_STR

      # 5️⃣ 构建
      - name: Build
        run: npm run build

      # 6️⃣ 发布到 pkg.pr.new
      - name: Publish preview to pkg.pr.new
        id: pkgpr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx --yes pkg-pr-new publish --comment=off --compact > output.txt
          INSTALL_URL=$(grep -oE 'https://pkg\.pr\.new/[^[:space:]`]+' output.txt | head -n 1)
          echo "install_url=$INSTALL_URL" >> $GITHUB_OUTPUT

      # 7️⃣ PR 评论（覆盖更新）
      - name: Comment preview info
        uses: thollander/actions-comment-pull-request@v3
        with:
          comment-tag: release-preview
          mode: upsert
          reactions: rocket
          message: |
            ## 🚀 Release PR 预览包已发布

            **版本**
            ```
            ${{ env.PREVIEW_VERSION }}
            ```

            **安装命令**
            ```bash
            npm i ${{ steps.pkgpr.outputs.install_url }}
            ```
